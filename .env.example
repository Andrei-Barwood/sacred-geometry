# Copia a .dev.vars para `wrangler pages dev` (no subas secretos al repo).
# Formato: KEY=value (sin comillas salvo que el valor las necesite)

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:tu-email@ejemplo.com
NOTIFY_SECRET=
