# Functions Directory

This directory is intentionally empty. This site is a static site and does not use Cloudflare Pages Functions.

This directory exists to prevent Cloudflare Pages from attempting to deploy functions when none are present.
